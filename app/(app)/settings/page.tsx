import SettingsTabSwitcher from "@/components/settings/settings-tab-switcher";

export const metadata = {
  title: "Settings - Delivery Point | Admin",
};

export default function Settings() {
    return <SettingsTabSwitcher/>
}