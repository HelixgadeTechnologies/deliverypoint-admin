import ResetPasswordComponent from "@/components/reset-password"

export const metadata = {
    title: "Reset Password - Delivery Point | Admin"
}

export default function ResetPassword() {
    return <ResetPasswordComponent/>
}